# V0.3 验证记录

日期：2026-09-08。代码版本：0.3.1。语料：wwc-30-2026-09-08。

0.3.1 修订背景：用户报告公网 0.3.0 的教学调整请求显示通用错误。随后使用同一四年级小组阅读情境，实际公网调用成功；会话导出确认模型 gpt-5.6-sol，17:48:34–17:48:57 UTC，约 23 秒，input_tokens=11008、output_tokens=1504，其中 reasoning_tokens=516。该测试未开启交互记录，界面为 0 saved / 0 pending。它证明该部署存在成功调用路径，不能追溯先前失败的具体原因，也不是新修订的线上验收。

0.3.1 针对已确认的诊断缺陷保留安全错误分类，新增可选推理与超时配置，默认输出预算提高到 4000。已有云端显式配置优先于新默认值。

## 已通过

| 范围 | 结果与实际检查 |
| --- | --- |
| 内容完整性 | 30 份指南，151 条建议、14 个分项、600 个组件，1,161 个原文页面；全部步骤有可定位的来源。 |
| 原始资料 | 包中 33 份 PDF 的 SHA-256 与目录记录一致。 |
| 全量显示 | 在 Shiny 测试会话逐一打开 165 个行动，检查行动和来源输出。 |
| 检索 | 多音节解码、分数数轴、大学学业与非学业支持、课堂行为期待、代数例题五个问题均在前五项返回预期建议；不存在的词返回空结果。不是全库检索准确率评估。 |
| 来源校验 | 不存在的引用 ID、伪造引文、缺少引用的原文结论、不存在的行动均被拒绝。 |
| AI 调用链 | 使用测试 HTTP 响应，通过实际异步 worker、Responses 响应解析、引用校验和 UI 显示；两轮上下文、错误回答拒绝、清空对话后旧回答不再出现。未调用真实模型。 |
| 0.3.1 错误诊断 | 离线检查 HTTP 权限、模型、账单、限流、参数、服务商错误及原始消息排除；实际异步 worker 验证输出额度不足、超时和 API key 错误的 UI 分类及同意后的记录，失败不修改计划，未同意时不保存互动事件。 |
| 计划 | 按行动保存、来源切换不重置计划、旧版本输入被拒绝；TXT 包含教师行动、理由与来源。导出请求以完整输入快照传输，测试包含未等待输入 debounce 的新文字与新情境。 |
| 数据 | SQLite 同意后写入、重复事件幂等、停止后无新增、删除当前会话；不同会话互相隔离。请求中途才同意不会追溯记录该请求的回答。 |
| 缺少服务 | 本地显示 AI 尚未连接；public 模式缺少模型和持久化设置时拒绝启动。 |
| Supabase 密钥兼容 | 离线检查新版 `sb_secret_` 的 apikey 请求头、环境变量优先级和旧版 JWT fallback；真实实例仍待联调。 |
| 浏览器 | 桌面首页、读写与数学搜索、行动与来源工作区、计划填写保存、数据说明；390px 宽度下导航与表单可用，首页与工作区无横向溢出。行动到对话的焦点跳转经过真实浏览器检查。 |
| 部署材料 | 官方 rsconnect 生成 manifest；逐文件核对 checksum，排除密钥、本地库和用户数据；ZIP 使用相同运行文件清单。 |

复现：`Rscript tests/check_v03.R`、`Rscript tests/check_async.R`、`Rscript tests/check_ai_errors.R`、`python tests/validate_bundle.py`。JavaScript 通过 Node 的语法检查。R 测试进程不读取实际 `.Renviron`，使用独立临时数据库，异步测试只在测试进程替换 HTTP 返回值。

## 尚未验证 / 完成

- 0.3.1 云端重新部署与真实调用尚待执行。0.3.0 的一次成功不能证明各场景的质量、延迟和长期可靠性；先前那次失败的原因没有被旧版本记录。
- Supabase 实例上的 SQL、写入、删除和多实例限额：缺少专用项目和服务器 key。SQL 与应用适配已提供；`scripts/live_smoke.R` 可在配置后验证。
- 已访问用户发布的 Posit 公开网址；平台管理端、独立域名和 DNS 未检查或修改。
- 人工内容审核：本版使用现有完整草稿，保留尚未审核的真实状态；本次机器校验不替代人工核对。
- 无障碍全面评估、压力测试、长期可靠性与跨设备身份：不属于已通过的本地验收结果。

现场测试发现并修复了多段 AI 输出验证、Shiny 消息处理函数、刚输入即导出、过期计划保存和同意时点等问题。后续更换模型或修改语料后，仍需针对新版本进行相应检查。
