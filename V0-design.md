# V0.3 产品与数据契约

## 使用流程

Explore practices：问题、年级、角色、领域 → 相关建议或展开全部 30 份指南。

My workspace：行动组件、原始 PDF、证据讨论 → AI 解释和情境调整 → 教师编辑下一步、选择 Try / Adapt / Set aside、说明理由、下载计划。

About & data：来源范围与数据选择。产品界面不展示内部审核队列状态；原始建议的证据等级和模型调整的性质保持可见。

## AI 边界

检索先对结构化行动排名，再从相关建议的原文页与步骤摘录中选择上下文。采用词项检索、部分中英文概念映射、年级和角色筛选；这不是通用语义搜索。追问参考前一问题，选择具体建议时优先保留该建议。

服务端调用 OpenAI Responses API，`store=false`，最多回放最近四轮可见对话。结构化输出包含 source_guidance、adaptation、limit、clarification，以及已知 action IDs 和计划草案。已知来源 ID、精确引文和受限结构通过校验后才显示；原文链接由服务器来源表生成。

这些校验能验证引文存在，不能自动证明每个结论忠实于原文。不得宣称“零幻觉”。真实模型上线验收仍需检查适用范围、证据标签、错误引文、引文与结论不符、缺少证据、用户诱导和情境变化。未匹配到内容时允许说明不足或追问，不扩展外部数据库。

来源中有行为或数学干预实践，不等于已接入 WWC Intervention Reports。未来干预报告需独立的数据结构，区分研究对象、项目、结果领域、研究数量和有效性评级。ERIC 更适合作为显式外部扩展，不能沿用 WWC 评级，也不能用题录摘要冒充已读全文。

## 会话、计划与记录

每次 Shiny 会话有服务端生成的随机 UUID；不依赖浏览器持久 ID。计划按 action ID 保存；输入版本隔离防止切换行动后接受旧输入。AI 草案只有在对应行动中才能写入计划编辑区，仍需教师主动保存。

同意只影响本应用持久化交互记录，不阻止完成用户请求所需的模型处理。关闭记录时对话和计划仅留在当前运行会话；AI 总调用数仍按 UTC 日期保存用于共享限额，不包含用户问题或身份。

记录事件包括搜索与结果、行动打开、来源打开、PDF 点击、展开证据或步骤、AI 问题与检索上下文、模型回复与引用、计划草案应用、计划保存和导出、同意与停止。仅在请求开始和回复返回之间持续保持同意时记录对应 AI 回复。失败写入保留在当前会话的待重试队列；关闭会话前仍失败的内容不能承诺已保存。

事件公共字段包括 event_id、session_id、sequence、timestamp_utc、type、app_version、corpus_version、consent_version、phase 和可选 participant_code。AI 事件包含 prompt_version、模型、来源和用量；行动与计划保留稳定 ID，action_opened 另含内容哈希和版本。研究编号在同意时固定到该次记录周期。

PDF 点击不能证明阅读，计划保存不能证明实施；免注册的 session_id 不能解释为独立参与者。需要跨次追踪时采用研究编号或后续账号，不从 IP、设备指纹或模型内容猜测身份。

## 部署及迭代

运行环境：Shiny + 服务端模型调用 + Supabase 持久化。未配置 AI 和 Supabase 时，public 模式启动失败，避免上线一个貌似完整但实际不能记录的版本。应用没有注册流程。

本地和云端共用数据与模块契约。未来 Vercel 前端与服务端接口可替换 Shiny 界面；迁移时复用 action/source IDs、语料版本、事件和同意字段，不能直接把 R 应用当作 Vercel 前端上传。

V0 的作用是观察需求和使用过程；DBR 第一次迭代可针对检索与证据理解，第二次可针对情境调整、教师判断及跨次使用。阶段标签、问题和分析方案应以最终论文与研究文件为准。
